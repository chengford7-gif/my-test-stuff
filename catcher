import requests
import json
import pandas as pd
import numpy as np
from datetime import datetime, timedelta
import os
import traceback  # 👈 新增：这是你的侦探，用于记录崩溃现场

# 强制切换 matplotlib 为 Headless 模式
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
import matplotlib.dates as mdates
from matplotlib.ticker import FixedLocator, FixedFormatter

SYMBOL = "IREN"

# 💥 侦探逻辑：把所有操作塞进 try-except
def run_main_pipeline():
    try:
        # --- [ 这里是你原本的逻辑 ] ---
        # 保持你之前那套完整的绘图和 README 生成逻辑不变
        df = get_macro_weekly_data(SYMBOL)
        if df is not None and not df.empty:
            generate_calibrated_tradingview_chart(df)
            
            # ... (此处省略，请保持你之前那份完整的绘图逻辑代码不变) ...
            
            # 确保 README 写入部分也在这里
            print("✅ 脚本运行成功，准备同步...")
            
    except Exception as e:
        # 💥 如果崩溃，直接把完整报错打印到控制台，我们就知道原因了
        print("❌ 脚本崩溃，现场诊断信息如下：")
        traceback.print_exc()
        raise e  # 抛出异常让 GitHub Actions 捕获

# 补全辅助函数
def get_macro_weekly_data(symbol):
    # ... (保持原样) ...
    pass

def generate_calibrated_tradingview_chart(df):
    # ... (保持原样) ...
    pass

if __name__ == "__main__":
    run_main_pipeline()
